import back from '../assets/ss.jpg';
import folder from '../assets/folder.jfif';


const Main = () => {
  return (
    <div className="main">
    <img className='background' src={back} alt="BackGround"/>
    <img className='folder' src={folder} alt='folder'/>
     </div>
  )
}

export default Main